<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cinema">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Cinema">
        <activity android:name=".ui.QuanLyThongTin"></activity>
        <activity android:name=".XemTrailer" />
        <activity android:name=".ui.ChonGheActivity" />
        <activity android:name=".ui.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.MovieDetailActivity" />
        <activity android:name=".ui.DangKy" />
        <activity android:name=".ui.ChinhSuaThongTin" />
        <activity android:name=".ui.Login" />
        <activity android:name=".ui.DatVeActivity" />
        <activity android:name=".ui.PaymentActivity" />
    </application>

</manifest>